# simulator 브랜치 작업 프롬프트

담당 페이지: `simulator.html` (시뮬레이터)

**기획**: 사용자가 과거 매수 시점을 고르면, 그 시점 이후 "환율 반영한 비트코인
김프"의 수익률을 그래프로 보여주는 체험 도구. 테더 출시 이후로 기간을 제한하지
않아도 됨.

원래 README/스켈레톤에 있던 "리스크체크리스트 / 스프레드계산기 / 퀴즈"도 계속
스코프에 포함됩니다 — 지금 있는 매수 시점 시뮬레이터에 이어서 추가하는 "고도화"
단계로 진행하면 됩니다.

작업을 시작할 때 아래 프롬프트를 그대로 복사해서 Claude Code에 붙여넣고 시작하세요.
(뒤에 이어서 이번에 하고 싶은 구체적인 작업 내용을 덧붙이면 됩니다.)

---

## 복사해서 쓸 프롬프트

```
지금 simulator 브랜치에서 simulator.html 작업할 거야. 시작하기 전에 shared.css를
먼저 읽고, 거기 정의된 CSS 변수(색상/spacing/radius)와 클래스(.card, .stat-card,
.badge, .disclaim 등)를 최대한 재사용해줘. dashboard.html도 같이 열어보면
formula-box, simple-breakdown 같은 컴포넌트를 어떻게 쓰는지 참고할 수 있어.

지켜야 할 것:
1. 색상은 shared.css의 --frog/--accent/--amber/--text/--mut/--dim 등 변수만 쓰고
   새 색을 임의로 만들지 마.
2. 이 페이지는 "투자 조언"이 아니라 "원리를 직접 체험해보는 이해용 도구"야.
   "지금 사세요/파세요" 같은 문구나 뉘앙스는 절대 쓰지 마. 계산/시뮬레이션 결과를
   보여줄 땐 "이 결과는 특정 시점을 가정한 것이며 투자 조언이 아닙니다" 같은 안내
   문구를 결과 상단에 고정으로 넣어줘 (shared.css의 .disclaim 클래스 참고).
3. spacing/radius는 var(--sp-*), var(--r-*) 토큰만 쓰고 임의 px 값 넣지 마.
4. 상단 네비게이션(.topnav)은 다른 페이지와 완전히 동일한 마크업을 유지하고,
   탭 순서(홈-패턴분석-대시보드-시뮬레이터)와 활성 탭 표시도 그대로 둬.
5. shared.css 자체에 새 컴포넌트를 추가해야 하면, 파일 구조를 바꾸지 말고 맨 아래에
   기존 네이밍 규칙(BEM 느낌)을 따라 추가해줘. 다른 페이지도 같이 쓰는 파일이니까
   기존 클래스는 되도록 건드리지 마.

작업 끝나면 로컬 서버로 직접 열어서 눈으로 확인하고(콘솔 에러 없는지, 모바일 폭에서도
안 깨지는지), git commit 메시지는 "simulator: 설명" 형식으로 써줘 (feat/style/docs
같은 접두어 말고 브랜치 이름을 접두어로).
```

---

## 지금까지 구현된 것

- "당신이 그때 샀다면, 지금은 어떨까" — 과거 매수 시점을 슬라이더로 선택
- 선택한 시점부터 지금까지 "프리미엄 기준 누적수익" vs "환율 반영 실질 누적수익"
  두 선 비교 차트 (dashboard.html의 ④번 컴포넌트 재사용)
- 결과 아래 자동 요약 문장 ("환율이 실질 수익을 늘렸습니다/줄였습니다")
- 결과 상단에 투자 조언이 아니라는 안내 문구 고정 배치

`dashboard.html`의 `seededRandom` 로직을 재사용한 데모 데이터입니다. 실제 API 연동은
아직 안 되어 있습니다.

## 남은 것

- 리스크 체크리스트 (체크리스트 인터랙션)
- 스프레드 계산기
- 퀴즈 (통과 시 계산기 잠금 해제)
- 지금 데모 데이터는 `dashboard.html`의 범용 프리미엄 로직을 재사용한 거라 BTC
  전용이 아닙니다. 기획상 "비트코인 김프"가 맞으니, 실제 API 연동 시에는
  `getUsdtPremiumData()`가 아니라 **`getBtcPremiumData()`**를 쓰세요 (pattern.html과
  동일한 이유 — `docs/prompts/pattern.md`의 "shared.js 연동 방법" 참고).

